rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Photos are public to view; only you can upload or delete.
    match /records/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == "YOUR_UID";
    }
  }
}
